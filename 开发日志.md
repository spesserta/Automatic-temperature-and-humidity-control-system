### 一、介绍

采用WPF C# 依托于依赖注入，控制反转库DependencyInjection实现的开发解耦，结合CommunityToolkit.Mvvm、MahApps、LiveCharts、HandyControl、IconPacks等组件共同开发而成。<br>

硬件上以PLC为核心，结合多个传感器实现数据输入。

#### 1、采用技术

##### CommunityToolkit.Mvvm

一个现代快速且模块化的MVVM库，支持属性更改通知(ObserableObject)，命令支持(RelayCommand和AsyncRelayCommand)，消息传递、依赖注入、输入验证、源生成器、跨平台支持等。支持Winform和WPF等



##### LiveChart

做数据可视化的库，可以做各种图标

##### HandyControl

开源的自定义控件，比微软原生控件更好看

##### Sqllite

轻量型数据库，是ORM框架，提供LINQ查询、事务管理等操作。代码优先，数据库优先，有迁移功能，可以异步操作



#### 2、各种硬件部件

<img width="700" height="405" alt="image" src="https://github.com/user-attachments/assets/bcee6f9e-08f4-4185-9df8-5519ddae6350" />


所有的部件都是为了保持室内的温湿度而设计的。


#### 3、项目创建

选择.NET Core创建即可

- CompanyShell层：UI层以及相关业务层
- CompanyCore层：其他项目可能引用的类库
- CompanyDatabase层：数据库抽象层
- CompanyDatabaseSqllite层：数据库实现层
- CompanyPlc层：PLC抽象层
- CompanyPlcNmodbus层：Plc实现层


其中CompanyShell层中Views文件夹里面的ShellView作为启动串口，因此需要更改一下App.xaml，将默认启动串口删掉

```
<Application x:Class="CompanyShell.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CompanyShell">
    <Application.Resources>
         
    </Application.Resources>
</Application>
```

然后重写启动逻辑
```c#
using CompanyShell.DependencyInjection;
using CompanyShell.Views;
using Microsoft.Extensions.DependencyInjection;
using System.Configuration;
using System.Data;
using System.Windows;

namespace CompanyShell
{
    /// <summary>
    /// Interaction logic for App.xaml
    /// </summary>
    public partial class App : Application
    {
       
        public new static App Current => (App)Application.Current;
        //加载依赖注入的各种设施
        public IServiceProvider Services { get; private set; } = ServiceManager.Load();

        protected override void OnStartup(StartupEventArgs e) //启动App的时候执行这个
        {
            Services.GetService<ShellView>().Show();  //显示ShellView主窗体
        }

    }

}
```


#### 4、安装对应Nuget包


<img width="1027" height="707" alt="image" src="https://github.com/user-attachments/assets/569bb03e-5719-42bd-97d2-9e4fd53e77a3" />




### 二、制作

#### 1、主窗体shellview制作


主窗体使用的是HandyControl库，因此需要引入handyControl串口

```xml
    <!--第八行引入handyControl，第一行也得改一下-->
<hc:Window x:Class="CompanyShell.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CompanyShell.Views"
        mc:Ignorable="d"
        xmlns:hc ="https://handyorg.github.io/handycontrol"  
        Title="ShellView" Height="450" Width="800">
```
```c#
//ShellView.xaml
public partial class ShellView : HandyControl.Controls.Window  //改成HandyControl库的串口
{
    public ShellView()
    {
        InitializeComponent();
    }
}
```
```xml
//APP.xaml中加入handyControl官网中的快速启动复制文件
<Application.Resources>
    <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
            <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
        </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
</Application.Resources>

```

随后建立InitializeView和InitializeViewModel作为初始界面

```xml
<!--这是初始化的界面-->
    <UserControl x:Class="CompanyShell.Views.InitlizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompanyShell.Views"
             xmlns:vm ="clr-namespace:CompanyShell.ViewModels"
             d:DataContext="{d:DesignInstance vm:InitializeViewModel}"
             xmlns:hc ="https://handyorg.github.io/handycontrol"  
             FontSize="18"
             mc:Ignorable="d"  
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Background>
        <!--主界面蓝白灰渐变色-->
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="LightBlue" Offset="0"/>
            <GradientStop Color="White" Offset="0.5"/>
            <GradientStop Color="LightGray" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>
    
    <Grid>
            
    </Grid>
</UserControl>
```
将Model层的类库引入到View层
```c#
namespace CompanyShell.Views
{
    /// <summary>
    /// InitlizeView.xaml 的交互逻辑
    /// </summary>
    public partial class InitlizeView : UserControl
    {
        public InitlizeView()
        {
            InitializeComponent();
            //ViewModel中的数据给到View中
            DataContext = App.Current.Services.GetService<InitializeViewModel>();
        }
    }
}
```
ViewModel层的类库代码如下：
```c#
using CommunityToolkit.Mvvm.ComponentModel;
using CompanyShell.Views;
using Microsoft.Extensions.DependencyInjection;

namespace CompanyShell.ViewModels
{
    internal class ShellViewModel : ObservableObject  //这个ObservableObject是继承了INotifyPropertyChanged接口，是拿来进行UI与后台交互的
    {
        private object? content;
        /// <summary>
        /// 页面中想要显示的内容，在主界面的<ContentControl Content="{Binding Content}"/>转到这
        /// </summary>
        public object? Content
        {
            get {  return content; }
            set { SetProperty(ref content, value); }
        }
        public ShellViewModel()
        {
            //启动主界面的时候默认显示InitlizeView这个默认显示界面
            Content = App.Current.Services.GetService<InitlizeView>();
        }
    }
}
```
然后将初始界面引入到主界面ShellView中：
```xml
    <!--第八行引入handyControl，第一行也得改一下-->
<hc:Window x:Class="CompanyShell.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CompanyShell.Views"
        mc:Ignorable="d"
           xmlns:vm ="clr-namespace:CompanyShell.ViewModels"
           d:DataContext="{d:DesignInstance vm:ShellViewModel}"
        xmlns:hc ="https://handyorg.github.io/handycontrol"  
           Background="{StaticResource BackgroundBrush}"
           WindowStartupLocation="CenterScreen"
           ShowNonClientArea="True"
           Icon="/Images/logo.ico"
           FontSize="18"
           NonClientAreaBackground="{StaticResource BackgroundBrush}"
        Title="全自动温湿度控制系统" Height="450" Width="800">
    <Grid>
        <ContentControl Content="{Binding Content}"/>
        
    </Grid>
</hc:Window>
```


#### 2、初始窗体Initialize细节的制作


目前已经引入了初始窗体，效果如下：


<img width="1001" height="554" alt="image" src="https://github.com/user-attachments/assets/770c8a07-468b-467b-8b87-1d8379509eba" />


现在开始设计初始窗体


```xml
<!--这是初始化的界面-->
    <UserControl x:Class="CompanyShell.Views.InitlizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompanyShell.Views"
             xmlns:vm ="clr-namespace:CompanyShell.ViewModels"
             d:DataContext="{d:DesignInstance vm:InitializeViewModel}"
             xmlns:hc ="https://handyorg.github.io/handycontrol"  
             FontSize="18"
             mc:Ignorable="d"  
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="450" d:DesignWidth="800">
        
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
        
    <UserControl.Background>
        <!--主界面蓝白灰渐变色-->
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="LightBlue" Offset="0"/>
            <GradientStop Color="White" Offset="0.5"/>
            <GradientStop Color="LightGray" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>
    
    <Grid>
        <StackPanel Margin="100 0" VerticalAlignment="Center">
            <!--提示信息-->
            <hc:SimpleText Text="{Binding Content}" HorizontalAlignment="Center"/>
            <!--进度条动画-->
            <hc:LoadingLine Margin="0,30" Foreground="Black" Style="{StaticResource LoadingLineLight}"/>
            <!--重试按钮-->
            <Button Content="重试" HorizontalAlignment="Stretch" Height="45" FontSize="18"
                    Style="{StaticResource ButtonInfo}"
                    Visibility="{Binding ShowButton}"
                    Command="{Binding InitializeCommand}"/>

        </StackPanel>
    </Grid>
</UserControl>
```

```c#
//.cs文件
namespace CompanyShell.Views
{
    /// <summary>
    /// InitlizeView.xaml 的交互逻辑
    /// </summary>
    public partial class InitlizeView : UserControl
    {
        public InitlizeView()
        {
            InitializeComponent();
            //ViewModel中的数据给到View中
            DataContext = App.Current.Services.GetService<InitializeViewModel>();
        }
    }
}
```
```c#
//ViewModel层
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using System;
using System.Collections.Generic;
using System.Reflection.Metadata;
using System.Text;
using System.Windows;
using System.Windows.Input;

namespace CompanyShell.ViewModels
{
    internal class InitializeViewModel : ObservableObject
    {
        private object? content;
        /// <summary>
        /// 页面中想要显示的内容，在主界面的<ContentControl Content="{Binding Content}"/>转到这
        /// </summary>
        public object? Content
        {
            get { return content; }
            set { SetProperty(ref content, value); }
        }


        private Visibility? showButton;
        /// <summary>
        /// 是否显示重试按钮
        /// </summary>
        public Visibility? ShowButton
        {
            get { return showButton; }
            set { SetProperty(ref showButton, value); }
        }

        public ICommand? LoadedCommand { get; }
        public ICommand? InitializeCommand {  get;  }
        public InitializeViewModel()
        {
            LoadedCommand = new RelayCommand(OnLoadedCommand);
            InitializeCommand = new AsyncRelayCommand(OnInitializeCommand);
            Content = "正在初始化数据库...";
        }

        private void OnLoadedCommand()   //初始化背后执行的内容
        {
            
        }

        private async Task OnInitializeCommand()
        {
            await Task.Delay(500);
        }
    }
}
```



#### 3、设计数据库结构和操作接口


在CompanyDatabase层添加CommunityToolkit.Mvvm这个nuget包。<br>

然后新增Entities文件夹，里面放实体类基类，具体如下：
```c#
using CommunityToolkit.Mvvm.ComponentModel;
using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;
using System.Text;

namespace CompanyDatabase.Entities
{
    //实体类基类
    public abstract class EntityBase : ObservableObject
    {
        protected int id;
        /// <summary>
        /// 主键ID
        /// </summary>
        [Key]
        [DatabaseGenerated(DatabaseGeneratedOption.Identity)] //自动递增

        public int Id
        {
            get { return id; }
            set { SetProperty(ref id, value); }
        }

        /// <summary>
        /// 插入时间
        /// </summary>
        private DateTime insertDate = DateTime.Now;
        public DateTime InsertDate
        {
            get { return insertDate; }
            set { SetProperty(ref insertDate, value); }
        }
    }
}
```

```c#
using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations.Schema;
using System.Text;

namespace CompanyDatabase.Entities
{
    //数据表-用户表实体
    [Table(nameof(UserEntity))]
    public class UserEntity : EntityBase
    {
        /// <summary>
        /// 用户名
        /// </summary>
        private string userName = string.Empty;
        public string UserName 
        {
            get {  return userName; }
            set { SetProperty(ref userName, value); }
        }

        /// <summary>
        /// 密码
        /// </summary>
        private string password = string.Empty;
        public string Password
        {
            get { return password; }
            set { SetProperty(ref password, value); }
        }


        /// <summary>
        /// 用户权限（1是管理0是用户）
        /// </summary>
        private int role = 0;
        public int Role
        {
            get { return role; }
            set { SetProperty(ref role, value); }
        }

    }
}
```
实体类基类增加完成后，现在添加对应的数据库操作类
```c#
using System;
using System.Collections.Generic;
using System.Text;

namespace CompanyDatabase
{
    //数据库操作类的基接口
    public interface IRepository<TEntity> where TEntity : class
    {
        bool Insert(TEntity entity); //增加
        bool Update(TEntity entity); //修改
        bool Delete(TEntity entity); //删除
        List<TEntity> GetAll();  //查询
    }
}
```
```c#
using CompanyDatabase.Entities;
using System;
using System.Collections.Generic;
using System.Text;

namespace CompanyDatabase
{
    //用户类操作类的接口
    public interface IUserRepository : IRepository<UserEntity>
    {
        UserEntity? Search(string username); //搜索
        bool Login(string username,string password ,out UserEntity user);  //登录
    }
}
```



到此数据库抽象层设计完毕，接下来是数据库实现层的设计：<br>

打开CompanyDatabaseSqllite这个项目，在这里添加Sqllite这一系列的nuget包



<img width="721" height="461" alt="image" src="https://github.com/user-attachments/assets/f95b9422-e15a-4d11-8f01-585871f88cbe" />

添加完后新建数据库上下文类表示数据库的基本信息：
```c#
using CompanyDatabase.Entities;
using Microsoft.EntityFrameworkCore;
using System;
using System.Collections.Generic;
using System.Text;

namespace CompanyDatabaseSqllite
{
    //数据库的上下文
    public class SqliteDbContext : DbContext
    {
        public DbSet<UserEntity> Users { get; set; }
        protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
        {
            base.OnConfiguring(optionsBuilder);

            //设置SQLite数据库的路径（系统自动设置）
            string databasePath = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),this.GetType().Name , "database.db");
            //创建目录
            Directory.CreateDirectory(Path.GetDirectoryName(databasePath));
            //使用数据库
            optionsBuilder.UseSqlite($"Data Source = {databasePath}");
            File.WriteAllText("database.txt", databasePath);

        }
    }
}
```
然后UserRepository类把用户操作的接口实例化：

```c#
using CompanyDatabase;
using CompanyDatabase.Entities;
using System;
using System.Collections.Generic;
using System.Text;

namespace CompanyDatabaseSqllite
{
    //实现用户表的操作
    public class userRepository : IUserRepository
    {
        private SqliteDbContext db = new();
        public bool Delete(UserEntity entity)  //删除
        {
            db.Entry(entity).State = Microsoft.EntityFrameworkCore.EntityState.Deleted;
            return db.SaveChanges() > 0;
        }

        public List<UserEntity> GetAll()  //获取User表所有数据
        {
            return db.Users.ToList();
        }

        public bool Insert(UserEntity entity)  //新增用户
        {
            db.Entry(entity).State = Microsoft.EntityFrameworkCore.EntityState.Added;
            return db.SaveChanges() > 0;
        }

        public bool Login(string username, string password, out UserEntity user)  //登录
        {
            user = null;
            var list = db.Users.Where(t => t.UserName.Equals(username) && t.Password.Equals(password)).ToList();
            if(list!=null && list.Count > 0)
            {
                user = list.FirstOrDefault() ?? new();  //选查找到的第一条，没有就new一个
                return true;
            }
            return false;
        }

        public UserEntity? Search(string username)  //按名查找
        {
            return db.Users.Where(t=>t.UserName == username).FirstOrDefault();
        }
        

        public bool Update(UserEntity entity)  //更新用户
        {
            db.Entry(entity).State = Microsoft.EntityFrameworkCore.EntityState.Modified;
            return db.SaveChanges() > 0;
        }
    }
}
```


在工具-Nuget管理包设置-程序包管理器设置以此运行以下指令即可生成迁徙文件
```
//生成迁移文件
Add-Migration InitialCreate   
Update-Database
```

这样就生成了迁移文件,此时就算是把database.sql删掉了,在下一次打开的时候也会自动生成实例化

<img width="1892" height="741" alt="image" src="https://github.com/user-attachments/assets/12fcfd6f-a79c-4efb-9d50-e7c8aa2ebf3a" />





使用everything查找数据库名称可以知道数据库建在哪了，有了路径后可以使用SQLite Expert打开这个数据库，这样就做到了数据库可视化



<img width="1512" height="852" alt="image" src="https://github.com/user-attachments/assets/eefde6c7-70ef-4353-ab4b-781d48e37e46" />



<img width="1523" height="843" alt="image" src="https://github.com/user-attachments/assets/72b1e9cb-ff08-425e-b51d-f93e1aeeb8d7" />



数据库搞好后在CompanyShell中的ServiceManager.cs文件中注册数据库层的文件，这样在程序打开时就会自动进行注册

```c#
namespace CompanyShell.DependencyInjection
{
    //这是一个静态工厂类，专门负责构建依赖注入容器。
    public class ServiceManager
    {
        public static IServiceProvider Load()
        {
            var services = new ServiceCollection();

            //在这里可以注册各种服务
            //注册UI层
            services.AddSingleton<ShellView>();
            services.AddSingleton<ShellViewModel>();
            services.AddSingleton<InitlizeView>();
            services.AddSingleton<InitializeViewModel>();

            //注册数据库层
            services.AddSingleton<IUserRepository, IUserRepository>();

            //注册业务层

            return services.BuildServiceProvider();
        }
    }
}
```


#### 4、创建用户，默认游客用户登录


























































